// Contact values and document links — identical across locales, so kept
// separate from /locales/*.json rather than duplicated in both languages.
// Fill these in once available; every empty value renders as a placeholder
// on the site until then. See README.md for details.

export const EMAIL = "";
export const PHONE = "";
export const LINKEDIN_URL = "";

export const CV_FR_URL = "";
export const CV_EN_URL = "";
export const THESIS_PDF_URL = "";

// Portrait photo shown in the hero. Drop a file in /public/images/ and set the
// path here (e.g. "/images/portrait.jpg"). Empty renders a monogram placeholder.
export const PORTRAIT_URL = "";
